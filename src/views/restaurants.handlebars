<div class="container vw-100 d-flex flex-wrap align-items-center flex-column">

<h3>Restaurants</h3>

<div class="row row-cols-2 row-cols-md-4 g-4 mb-5">
  <div>
    {{#each restaurantsData as |restaurant|}}
    <div>{{> restaurant-card}}</div>
    {{/each}}
  </div>
</div>


</div>